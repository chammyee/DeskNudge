Drop custom artwork here (all optional — SF Symbol fallbacks are used otherwise):

- MenuBarIcon.pdf        menu-bar icon, black-on-transparent vector, ~18pt square
- MenuBarIconPaused.pdf  menu-bar icon shown while paused/disabled

These are loaded as template images (macOS tints them for light/dark menu bars).
The Dock/Finder app icon is separate: put AppIcon.icns in ../../../scripts/.
